# Notebooks

`01_figures.ipynb` displays the TOY figures under `results/figures/`.
It is not the research product. Generate artefacts with:

```
make toy
```

which runs the synthetic leakage experiment, the protocol benchmark,
and `scripts/render_figures.py`.
